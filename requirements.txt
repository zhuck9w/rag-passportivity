# notion-client 2.6.0+ перешёл на API 2025-09-03 (data sources) и убрал
# databases.query (проверено 2026-07-09: 2.5.0 — последняя версия с ним) —
# не поднимать версию без переписывания notion_reader.py и discover.py
notion-client>=2.3,<2.6
slack-bolt>=1.20
supabase>=2.6
anthropic>=0.40
voyageai>=0.3
python-dotenv>=1.0
pytest>=8.0
